# Supabase Cloud Sync Configuration
#
# To enable cloud sync:
# 1. Create a free Supabase project at https://supabase.com
# 2. Go to Settings > API to get your URL and anon key
# 3. Create the table by running this SQL in the SQL Editor:
#
#    CREATE TABLE time_tracker_data (
#      device_id TEXT PRIMARY KEY,
#      data JSONB NOT NULL,
#      updated_at TIMESTAMPTZ DEFAULT NOW()
#    );
#
#    -- Enable Row Level Security
#    ALTER TABLE time_tracker_data ENABLE ROW LEVEL SECURITY;
#
#    -- Allow all operations for anonymous users (data is keyed by device_id)
#    CREATE POLICY "Allow all operations" ON time_tracker_data
#      FOR ALL USING (true) WITH CHECK (true);
#
# 4. Copy this file to .env and fill in your credentials

VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
